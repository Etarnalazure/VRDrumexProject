using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FootPedalCheatScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //I do nothing, I am simply a hacky and quick way to get the foot pedals collision for a condition
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
